import test from 'node:test'; import assert from 'node:assert/strict';
import { DEFAULT_CONFIG } from '../src/config.js'; import { DEFAULT_CRITERIA,mergeCriteria } from '../src/criteria.js'; import { evaluateCandidate } from '../src/strategy.js';
const candidate={gainerRank:9,change24h:1,contractType:'PERPETUAL',quoteAsset:'USDT',quoteVolume:1e9,spreadBps:1,currentPrice:9,threeDayOpen:8,candleAgeHours:60,rangePosition:.8,resistanceQualified:true,failedRetests:2,bearishRejection:true,supportBroken:true,momentum3m:'bearish',momentum5m:'bearish',oneRRealistic:true};
test('disabled criteria are excluded from eligibility',()=>{const result=evaluateCandidate(candidate,DEFAULT_CONFIG,{...DEFAULT_CRITERIA,topFiveGainer:false,gainThreshold:false});assert.equal(result.eligible,true);assert.deepEqual(result.excludedChecks,['topFiveGainer','gainThreshold','momentum3m','momentum5m']);});
test('persisted disabled criteria survive merge',()=>{const result=mergeCriteria({gainThreshold:false});assert.equal(result.gainThreshold,false);assert.equal(result.topFiveGainer,true);});
test('disabled rule also excludes its missing threshold',()=>{const config={...DEFAULT_CONFIG,minimum24hGainPct:null};const result=evaluateCandidate({...candidate,gainerRank:1},config,{...DEFAULT_CRITERIA,gainThreshold:false});assert.equal(result.missingConfiguration.includes('minimum24hGainPct'),false);});
